[project]
name = "rpmail"
version = "0.3.0"
description = "rpMail — Restore Privacy Mail (IMAP/POP3/SMTP client, Continuum vortice)"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Raskul / Restore Privacy" }]

[project.scripts]
rpmail = "rpmail.__main__:main"

[tool.setuptools.packages.find]
include = ["rpmail*"]
